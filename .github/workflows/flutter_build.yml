on:
  push:
env:
  APP_NAME: ''
  archive_path: ''
jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: macos-latest
          - os: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
            channel: 'stable'
            cache: true
      - run: flutter --version

      - run: flutter build macos
        if: startsWith(matrix.os, 'macos')
      - run: flutter build windows
        # √  Built build\windows\x64\runner\Release\frp_http_flutter_client.exe (0.1MB).
        if: startsWith(matrix.os, 'windows')

      - run: ls build/macos/Build/Products/Release
        if: startsWith(matrix.os, 'macos')
      - run: ls build/windows/x64/runner/Release
        if: startsWith(matrix.os, 'windows')

      - name: Set up env
        run: echo "APP_NAME=$(grep 'name:' pubspec.yaml | awk '{print $2}')" >> $GITHUB_ENV

      - name: Archive macos
        if: startsWith(matrix.os, 'macos')
        run: |
          tar -czf ${{ env.APP_NAME }}.tar.gz build/macos/Build/Products/Release/${{ env.APP_NAME }}.app
          echo "archive_path=${{ env.APP_NAME }}.tar.gz" >> $GITHUB_ENV
      - name: Archive windows
        if: startsWith(matrix.os, 'windows')
        run: |
          7z a -tzip ${{ env.APP_NAME }}.zip build/windows/x64/runner/Release
          echo "archive_path=${{ env.APP_NAME }}.zip" >> $GITHUB_ENV

      - name: Send mail
        uses: dawidd6/action-send-mail@v3
        with:
          connection_url: ${{secrets.MAIL_CONNECTION}}
          subject: Flutter CI
          body: 'Build completed. file: ${{ env.archive_path }}'
          to: i@oo1.dev
          from: Flutter Build
          attachments: ${{ env.archive_path }}
          priority: high
